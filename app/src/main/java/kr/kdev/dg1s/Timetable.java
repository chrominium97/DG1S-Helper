package kr.kdev.dg1s;

import android.app.Activity;


public class Timetable extends Activity {
    /*
    GridView timetable;
	Adapter_Timetable ttadapter;
	
	@Override
    public void onCreate(Bundle savedInstanceState) {
		super.onCreate(savedInstanceState);
		setContentView(R.layout.timetable);
		
		timetable = (GridView) findViewById(R.id.timetable);
		ttadapter = new Adapter_Timetable(this);
		timetable.setAdapter(ttadapter);
		
		timetable.setOnItemLongClickListener(this);
	}

	@Override
	public boolean onItemLongClick(AdapterView<?> parent, View v, int position, long id) {
		Toast.makeText(this, "당신은 "+position+"을 선택하셨습니다.", Toast.LENGTH_SHORT).show();
		new AlertDialog.Builder(Timetable.this)
			.setTitle("시간표 편집하기")
			.setItem();
		return false;
	}
	*/
}